      # ============================================================
      # Create normal Android launcher icon
      # NO adaptive icon
      # NO flutter_launcher_icons
      # ============================================================

      - name: Create Android Launcher Icon
        shell: bash
        run: |
          set -e

          RES="android/app/src/main/res"

          echo "Removing ALL old launcher icon resources..."

          rm -rf "$RES"/mipmap-*
          rm -rf "$RES"/drawable-anydpi*
          rm -rf "$RES"/drawable*/ic_launcher*
          rm -rf "$RES"/drawable*/ic_launcher_round*

          echo "Creating normal mipmap directories..."

          mkdir -p "$RES/mipmap-mdpi"
          mkdir -p "$RES/mipmap-hdpi"
          mkdir -p "$RES/mipmap-xhdpi"
          mkdir -p "$RES/mipmap-xxhdpi"
          mkdir -p "$RES/mipmap-xxxhdpi"

          echo "Copying app icon..."

          cp assets/icon/app_icon.png \
            "$RES/mipmap-mdpi/ic_launcher.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-hdpi/ic_launcher.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-xhdpi/ic_launcher.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-xxhdpi/ic_launcher.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-xxxhdpi/ic_launcher.png"

          echo "Creating round icon..."

          cp assets/icon/app_icon.png \
            "$RES/mipmap-mdpi/ic_launcher_round.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-hdpi/ic_launcher_round.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-xhdpi/ic_launcher_round.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-xxhdpi/ic_launcher_round.png"

          cp assets/icon/app_icon.png \
            "$RES/mipmap-xxxhdpi/ic_launcher_round.png"

          echo ""
          echo "Launcher icons created:"
          find "$RES" \
            -type f \
            \( -name "ic_launcher.png" -o -name "ic_launcher_round.png" \) \
            | sort